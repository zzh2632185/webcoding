{
  "name": "webcoding",
  "version": "2.1.0-zzh.1",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.js",
    "regression": "node scripts/regression.js",
    "contract:cli": "node scripts/cli-contract.js",
    "contract:protocol": "node scripts/protocol-contract.js",
    "test": "npm run regression && npm run contract:cli && npm run contract:protocol"
  },
  "dependencies": {
    "mammoth": "^1.12.0",
    "ws": "^8.21.0",
    "xlsx": "^0.18.5"
  }
}
